<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.NFCVCardWriter" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary_purple</item>
        <item name="colorPrimaryVariant">@color/deep_purple</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/primary_pink</item>
        <item name="colorSecondaryVariant">@color/magenta</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/deep_purple</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/background_gray</item>
    </style>
</resources>